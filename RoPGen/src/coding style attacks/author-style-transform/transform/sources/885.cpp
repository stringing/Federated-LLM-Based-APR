#include<iostream>
using namespace std;
int main () {
    int n, a, b; 
    double x;
    cin >> n >> a >> b;
    x = (n * a - 600.0 ) / (b - a);
    if (x == double) {
        x++;
    }
    cout << x << endl;
    return 0;
}
