#include <iostream>
#include <cstdio>
using namespace std;
int main() {
    freopen("plalindrome.in", "r", stdin);
    freopen("plalindrome.out", "w", stdout);
    int n;
    cin >> n;
    int a, b, c;
    for(int i = 0; a * a <= n; i++){
        for(int j = 0;a * a + b * b <= n; j++){
            for(int k = 0;a * a + b * b + c * c; k++){
            
                }
            }
        }
    }
