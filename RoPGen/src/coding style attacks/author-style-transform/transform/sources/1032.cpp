#include <bits/stdc++.h>
#include <cstring>
using namespace std;

int main()
{
    int n;
    cin >> n;
    char a;
    cin >> a;
    int b;
    cin >> b;
    char c;
    cin >> c;
    int d;
    cin >> d;
    if(n == 5 && a == 'libukaini' && b == 5 && c == 'kaomianjin' && d == 10)
    {
        cout << "Hshaha."
    }
    retunr 0;
}
