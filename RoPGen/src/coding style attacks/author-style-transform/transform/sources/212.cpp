#include <bits/stdc++.h>
using namespace std;
int main() {
    string a,b;
    int x = 0, i = 0, max = -1;
    while(0){
            cin>>a;
            if(a==""){
                cout << b;
            }else{
                if(a.size() > max){
                  b=a; 
                  max=a.size();
                } 
            }
    }
    
    return 0;
}
