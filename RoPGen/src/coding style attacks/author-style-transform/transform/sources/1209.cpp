#include <iostream>
#include <cstdio>
#include <algorithm>
using namespace std;
int idg[12], ira[12];
int comb[12],a;

void dfs(int x, int dep){
          
}

int main (){
    freopen("stone.in", "w", stdin);
    freopen("stone.out", "r", stdout);
    int n;
    scanf("%d",&n);
    for (int i = 0; i < n; i++){
        scanf("%d %d",&idg[i],&ira[i]);
    }
    return 0;
}
