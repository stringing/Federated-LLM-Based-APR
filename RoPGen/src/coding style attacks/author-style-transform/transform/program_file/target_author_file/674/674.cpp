#include <iostream>
using namespace std;   
int main() {
    double n , a , b;
    int  min_int;
    double s;
    cin >> n >> a >> b;
    min = (n * a - 600) / (b - a);
    min_int = min;
    if(min != min_int) {
        min_int++;
    }
    cout << min_int << endl;
    return 0;
}

