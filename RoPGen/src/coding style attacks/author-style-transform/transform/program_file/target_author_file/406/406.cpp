#include <bits/stdc++.h>
using namespace std;
int main() {
    freopen("bigadd.in", "r", stdin);
    freopen("bigadd.out", "w", stdout);
    long long n;
    cin >> n;
    cout << n + 2018;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
