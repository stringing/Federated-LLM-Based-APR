#include <iostream>
#include <string>
using namespace std;
int main(){
    string s, cnt;
    int sum;
    getline(cin, s);
        if (s[i] != ' ') {
            sum++;
        } else {
            cnt[i] = sum;
        }
      cout << cnt << endl;
    return 0;
}
